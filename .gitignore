# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Local agent state (Claude Code)
.claude/

# Image working files — commit exported/optimized images, not originals/sidecars
*.xcf
*.psd
*.CR2
*.NEF
*.ARW
*.xmp

# Phone originals: HEIC embeds GPS. NEVER commit — run scripts/garden.py convert,
# which strips metadata and writes a web JPEG beside the record. Commit the JPEG only.
*.HEIC
*.heic
*.HEIF
*.heif
